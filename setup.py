from setuptools import setup

setup(
    name='scheduler',
    version='0.1',
    description='Scheduler service that ensures the buffer streams are sent through an specific dataflow',
    author='Felipe Arruda Pontes',
    author_email='felipe.arruda.pontes@insight-centre.org',
    packages=['scheduler'],
    zip_safe=False
)
